export enum Values {
    createType = "create",
    inviteType = "invite",
}

export enum AppPermissions {
    view = "view",
    edit = "edit",
    approve = "approve",
    manage = "manage",
    delete = "delete",
}

export enum AppStatus {
    active = "Active",
    inactive = "Inactive",
    all = "All",
}

export enum Apps {
    slack = "slack",
    figma = "figma",
    confluence = "confluence",
    msOffice = "msOffice",
}